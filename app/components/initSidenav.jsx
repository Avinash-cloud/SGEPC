import { useEffect } from "react";

const AdminMenu = () => {
//   ependency array means it runs once when component mounts

  return (
    <ul className="admin-menu">
      {/* Your menu items here */}
    </ul>
  );
};

export default AdminMenu;
